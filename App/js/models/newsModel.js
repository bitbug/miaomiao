define(['jquery', 
		'underscore', 
		'backbone'
], function($, _, Backbone) {
	var NewsModel = Backbone.Model.extend({
	})
    
    //usually returning the object you created...
    return new NewsModel()
});