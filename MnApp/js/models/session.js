define(['jquery', 
		'underscore', 
		'backbone'
], function($, _, Backbone) {
	var SessionModel = Backbone.Model.extend({
	})
    
    //usually returning the object you created...
    return SessionModel
});